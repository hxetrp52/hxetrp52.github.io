# 사이트 내부 글쓰기 기능 구현 계획

## 1) 목표

- 현재처럼 로컬에서 `_posts/*.md` 파일을 직접 만들지 않아도, 블로그 페이지 안에서 글을 작성하고 발행할 수 있게 만든다.
- 비밀번호 인증 전에는 글쓰기 기능을 숨기고, 인증 성공 시에만 **글 작성하기** 버튼과 편집 화면을 노출한다.
- 작성 완료 시 GitHub 저장소에 새 포스트 파일이 생성되도록 자동화한다.

---

## 2) 핵심 요구사항 정리

### 기능 요구사항

1. 비밀번호 입력 UI 제공
2. 비밀번호 검증 성공 시 관리자 권한 세션 부여
3. 권한 세션이 있을 때만 글쓰기 버튼 활성화
4. 제목/본문/카테고리/태그/공개일 입력 가능
5. 발행 시 Jekyll 포맷(front matter + markdown)으로 파일 생성
6. 생성된 파일을 GitHub 저장소 `_posts/` 경로에 커밋
7. 배포 완료 후 블로그에 글 반영

### 비기능 요구사항

- 비밀번호는 클라이언트 코드에 노출되면 안 됨
- 인증/발행 API는 서버 측에서만 실행
- CSRF/브루트포스/입력 악용에 대한 기본 방어 필요
- 실패 시 사용자에게 원인과 재시도 방법 제공

---

## 3) 권장 아키텍처

정적 Jekyll 사이트 특성상, “사이트 내부 작성”은 **프론트엔드 + 서버리스 API** 조합으로 구현한다.

### 구성 요소

1. **프론트엔드(Jekyll 페이지)**
   - `/write/` 같은 전용 페이지 추가
   - 비밀번호 입력 폼 + 글쓰기 에디터 폼
   - 로그인 상태에 따라 버튼/폼 표시 제어

2. **인증 API (서버리스)**
   - 비밀번호를 서버 환경변수와 비교 검증
   - 성공 시 짧은 수명의 서명 토큰(JWT/세션 쿠키) 발급

3. **발행 API (서버리스)**
   - 토큰 검증 후 글 데이터 수신
   - front matter 포함 markdown 문자열 생성
   - GitHub API로 `_posts/YYYY-MM-DD-slug.md` 파일 생성 커밋

4. **배포**
   - 커밋 푸시 후 기존 GitHub Pages/Jekyll 빌드 파이프라인으로 자동 반영

---

## 4) 세부 기능 설계

### A. 인증/권한 흐름

1. 사용자가 비밀번호 입력 후 “권한 얻기” 클릭
2. 프론트가 `/api/auth/login` 호출
3. 서버에서 비밀번호 검증
4. 성공 시 HttpOnly + Secure 쿠키(권장) 또는 짧은 JWT 반환
5. 프론트에서 로그인 상태 확인 후 “글 작성하기” 버튼 활성화
6. `/api/auth/me`로 세션 유효성 확인 가능하게 구성

### B. 글쓰기 UI

입력 항목:
- 제목
- 본문(Markdown)
- 카테고리(복수 가능 여부 결정)
- 태그(쉼표 구분)
- 공개일(기본: 현재시간)
- 초안 여부(draft)

보조 기능:
- slug 자동 생성(한글 제목 처리 규칙 포함)
- front matter 미리보기
- 유효성 검사(제목/본문 필수, 태그 길이 제한 등)

### C. 발행 처리

1. 폼 제출 시 `/api/posts/create` 호출
2. 서버에서 입력 정규화 및 검증
3. 파일명 생성: `YYYY-MM-DD-slug.md`
4. 파일 내용 생성:
   - YAML front matter
   - 본문 markdown
5. GitHub Contents API로 `_posts/<category>/...` 또는 `_posts/...`에 커밋
6. 성공 응답에 생성된 URL/경로 반환

### D. 에러/중복 처리

- 동일 파일명 존재 시:
  - slug 뒤에 `-2`, `-3` suffix 자동 부여
- GitHub API 실패 시:
  - 사용자에게 “임시 저장 후 재시도” 안내
- 인증 만료 시:
  - 재로그인 요구

---

## 5) 보안 설계(중요)

1. **절대 금지**
   - 비밀번호를 JS 소스에 하드코딩
   - GitHub Personal Access Token을 프론트에 노출

2. **필수 적용**
   - 서버 환경변수로 비밀번호/토큰 관리
   - 로그인/발행 API rate limit
   - 입력 길이 제한, 허용 문자 검증, 기본 sanitize
   - CSRF 방지(동일사이트 쿠키 + 토큰 검증)
   - 보안 헤더 적용(CSP, X-Frame-Options 등 가능한 범위)

3. **권한 최소화**
   - GitHub 토큰은 해당 repo의 필요한 권한만 부여
   - 가능하면 GitHub App 사용으로 권한/회전 관리

---

## 6) 구현 단계

### 1단계: UI 뼈대
- `/write/` 페이지 생성
- 로그인 전/후 상태 UI 분리
- 글쓰기 폼 기본 필드 구축

### 2단계: 인증 API
- `/api/auth/login`, `/api/auth/me`, `/api/auth/logout` 구성
- 비밀번호 검증 + 세션 토큰 처리

### 3단계: 발행 API
- `/api/posts/create` 구현
- markdown/front matter 생성 로직 작성
- GitHub API 연동으로 파일 커밋

### 4단계: 연결 및 예외처리
- 프론트와 API 연동
- 에러 메시지, 재시도, 중복 slug 처리

### 5단계: 검증/운영 준비
- 인증 우회 불가 확인
- 정상 발행 후 실제 글 노출 확인
- 비밀값/환경변수/배포 설정 문서화

---

## 7) 테스트 항목

### 인증
- 올바른 비밀번호 로그인 성공
- 틀린 비밀번호 로그인 실패
- 세션 만료 후 기능 차단 확인

### 권한 제어
- 비로그인 상태에서 글쓰기 버튼 비활성/미노출
- 로그인 상태에서만 발행 API 호출 허용

### 글 발행
- 정상 입력 시 `_posts` 파일 생성 확인
- 한글 제목/특수문자 slug 처리 확인
- 같은 제목 반복 작성 시 충돌 회피 확인

### 보안/안정성
- 과도한 로그인 시도 제한 동작 확인
- 본문에 악성 스크립트 문자열 입력 시 안전 처리 확인
- GitHub API 장애 시 오류 안내 및 데이터 유실 최소화 확인

---

## 8) 운영 방안

- 비밀번호 주기적 변경
- 발행 API 로그(성공/실패/요청자 정보 최소 범위) 수집
- 잘못 발행된 글 롤백 절차(커밋 revert) 준비
- 장기적으로는 단일 비밀번호 방식에서 계정 기반 인증으로 확장 검토

---

## 9) 권장 기술 선택

- 프론트: 기존 Jekyll + 최소 JS(현재 테마 유지)
- 백엔드/API: GitHub와 연동 쉬운 서버리스 환경
  - 예: Cloudflare Workers / Vercel Functions / Netlify Functions 중 1개
- 저장: GitHub Contents API 기반 파일 커밋

---

## 10) 주의사항 (현실적 제약)

- 순수 GitHub Pages 정적 호스팅만으로는 “비밀번호 검증 후 권한 부여 + 저장소 쓰기”를 안전하게 처리할 수 없다.
- 따라서 반드시 **서버리스 API(비공개 환경변수 사용 가능)**를 함께 도입해야 한다.
- 이 구조를 쓰면 “사이트 내부 작성” 경험은 제공하면서도, 보안상 위험한 클라이언트 하드코딩을 피할 수 있다.

---

## 11) 현재 구현과 원안(서버리스) 차이

- 현재 리포지토리에는 사용자의 빠른 사용을 위해 **클라이언트 단독 방식**(브라우저에서 GitHub API 직접 호출)으로 1차 구현이 반영되었다.
- 즉, 서버리스 인증 API 없이 `/write/`에서 비밀번호 해시 비교 후 토큰으로 바로 업로드한다.
- 이 방식은 구현/운영이 간단하지만, 원안 대비 보안이 약하다(해시 공개, 오프라인 추측 위험, 토큰 입력 의존).
- 향후 안정 운영을 위해서는 본 계획서 3~6장의 서버리스 아키텍처로 전환하는 것을 권장한다.
